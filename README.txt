#   Dependencies to download:
#   yarn add @react-navigation/stack
#   yarn add @react-navigation/native
#   expo install react-native-gesture-handler or yarn add react-native-gesture-handler
#   yarn add firebase@9.6.11
#   yarn add react-native-safe-area-context

#   To finalize installation of react-native-gesture-handler, add the following at the top (make sure it's at the top and there's nothing else before it) of your entry file, 
#   such as index.js or App.js:
#   import 'react-native-gesture-handler'